<div class="modal hide fade modal-category " tabindex="-1" role="dialog" ng-controller="ModalCategoryController">
    <div class="modal-dialog modal-dialog-scrollable modal-dialog-centered" role="document">
        <div class="modal-content container">
            <div class="modal-header">
                <h3 class="modal-title">Thông tin danh mục  </h3>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            </div>
            <form class="modal-body">
                <div class="form-group">
                    <label for="exampleInputEmail1">Tên danh mục<span class="text-danger">*</span></label>
                   <input class="form-control" id="category" type="text" ng-model="category.CATEGORY" placeholder="Nhập vào tên danh mục" /><br/>
                   <input id="ckeck1" class="form-check-input" type="checkbox" ng-model="category.ISACTIVE" style="transform:scale(1.5);margin-left:10px;" />
                   <label class="form-check-label" for="ckeck1" style="margin-left:25px"> &nbsp; Hoạt động</label>
                </div>
                @*<div class="form-group">
                    <input id="isSubCategory" type="checkbox" style="transform:scale(1.5); margin-left: 10px;" ng-model="category.ISSUBCATEGORY">
                    <label class="form-check-label" for="isSubCategory"> &nbsp; Là danh mục con</label>
                </div>

                <div class="form-group" ng-show="category.ISSUBCATEGORY">
                    <label for="categorySelect">Danh mục: </label>
                    <select class="form-control" id="categorySelect" ng-model="category.CATEGORYID">
                        <option value="" disabled selected>--Chọn danh mục--</option>
                        <option ng-repeat="item in ListCategory" value="{{item.CATEGORYID}}">{{item.CATEGORY}}</option>
                    </select>
                </div>*@
            </form>
            <div class="modal-footer">
                <div class="d-flex justify-content-between">
                    <button class="btn btn-success" ng-click="SaveCategory()">
                        Lưu và đóng
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>
